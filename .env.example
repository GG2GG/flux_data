# OpenAI API Configuration
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key-here

# OR use OpenRouter for access to multiple models (Claude, Llama, Mixtral, etc.)
# Get your API key from: https://openrouter.ai/keys
# OPENROUTER_API_KEY=sk-or-v1-your-openrouter-key-here

# Model Selection (optional, defaults to gpt-4o-mini)
# LLM_MODEL=gpt-4o-mini
# For OpenRouter, you can use: anthropic/claude-3.5-sonnet, meta-llama/llama-3-70b, etc.

# LLM Temperature (optional, 0-1, default 0.7)
# LLM_TEMPERATURE=0.7

# LLM Max Tokens (optional, default 1500)
# LLM_MAX_TOKENS=1500

# Notes:
# - Set either OPENAI_API_KEY or OPENROUTER_API_KEY (not both)
# - The system works without LLM (uses templates) but explanations are much better with LLM
# - OpenRouter gives access to many models including Claude, Llama, Mixtral
# - Recommended: gpt-4o-mini (fast, cheap) or anthropic/claude-3.5-sonnet (best quality)
